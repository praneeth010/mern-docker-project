version: "3.8"

services:
  backend:
    build: ./backend
    image: mern-backend   # ✅ add this
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    depends_on:
      - mongo

  frontend:
    build: ./frontend
    image: mern-frontend   # ✅ add this
    ports:
      - "80:80"
    depends_on:
      - backend

  mongo:
    image: mongo:6
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
